<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@color/background">

    <TextView
        android:id="@+id/iv_item_recipe_order_img"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="조리법 이미지"
        android:textAppearance="@style/head_20"
        android:background="@drawable/shape_gray_bright_fill_5_rect"
        android:padding="100dp"
        app:layout_constraintBottom_toTopOf="@id/tv_item_recipe_order_explain"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <TextView
        android:id="@+id/tv_item_recipe_order_explain"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="1. 재료를 씻는다~~~~"
        android:textAppearance="@style/body_bold"
        android:textSize="25dp"
        android:includeFontPadding="false"
        app:layout_constraintTop_toBottomOf="@id/iv_item_recipe_order_img"
        app:layout_constraintStart_toStartOf="@id/iv_item_recipe_order_img"
        app:layout_constraintEnd_toEndOf="@id/iv_item_recipe_order_img"
        app:layout_constraintBottom_toBottomOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>